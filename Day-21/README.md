### Markdown version with icons

### Preview

### 🐧 Linux Practice Lab Summary

In this lab, I learned several fundamental Linux commands used for system administration and file management. I used the `date` command to display the current system date, time, and timezone, and the `uptime` command to check how long the system had been running, the number of logged-in users, and the system load average. I also used the `pwd` command to identify my current location within the Linux filesystem.

### 📂 Absolute vs Relative Paths

A major part of the lab focused on creating directories using both Absolute and Relative Paths.

* 📍 Absolute Path — specifies the complete location from the root (`/`) of the filesystem, for example:

* 📍 Relative Path — specifies a location based on the current working directory, for example, creating `projects` while already inside `/var/opt`:

### 📄 File Creation with touch

I learned how to create files using the `touch` command. Examples include:

This creates `summary.txt` and five additional files (`file1` through `file5`) in a single command.

### ⭐ Most Useful Command

The most useful command in this lab was `pwd` because it helped me confirm my current directory while navigating the filesystem.

### 🆕 New Command Learned

The `touch` command was new to me, and I found it especially helpful for quickly creating one or multiple files without opening a text editor.

### 📝 Overall Takeaway

This lab improved my understanding of Linux filesystem navigation, directory creation, and file management using basic command-line tools such as `date`, `uptime`, `pwd`, `mkdir -p`, and `touch`.
